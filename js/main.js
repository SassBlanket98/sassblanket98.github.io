// No JS-driven behavior in the terminal design — the blinking cursor and
// pulse dot are pure CSS animation (see .cursor / .pulse-dot in style.css),
// and the nav collapses via flex-wrap rather than a toggle.
